---
- name: Install PHP(OS default package)
  package:
    name: "{{ php_install_package }}"
    state: "present"

- name: Install extra package.
  package:
    name: "{{ php_packages_extra }}"
    state: "present"
  when: php_packages_extra is defined
