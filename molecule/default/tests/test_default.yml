# Molecule managed

---
package:
  php:
    installed: true

command:
  version:
    exit-status: 0
    exec: "php -i | grep date.timezone"
    stdout:
    - date.timezone => Asia/Tokyo => Asia/Tokyo
    stderr: []
    timeout: 10000
